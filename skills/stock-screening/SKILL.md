---
name: stock-screening
description: 通过自然语言查询进行 A 股股票筛选，支持行情指标、技术形态、财务指标、行业概念等多条件组合筛选。返回符合条件的相关股票数据。当用户询问针对行情、财务数据、技术指标、行业概念等A股股票筛选相关问题时，必须使用此技能。
required_tools:
  - iwencai.screen
---

# A 股选股 Agent 规则

1. 先识别用户的行业、财务、行情、技术形态和数量条件。
2. 必须调用必需工具 `iwencai.screen` 获取候选股票，不得自行编造股票数据。
3. 查询结果为空时，最多使用一次 `call_type=retry` 的明确条件放宽重试，并说明调整内容。
4. 用户只要求筛选时，返回候选股票、命中条件、结果数量和问财数据来源。
5. 用户要求比较、分析或风险判断时，将候选集交给根编排器安排后续 Agent，不在本 Skill 内搜索 Web。
6. 「哪些板块领涨 / 行业涨跌幅排名」不是选股问题，应改走 `iwencai.industry.query`，不要用本选股工具硬答。
7. 必需工具未绑定或调用失败时，直接报告任务未完成，不得绕过工具调用外部 API。
8. 不输出保证收益、确定涨跌或直接交易指令。
9. 结果不足时明确列出数据缺口，不用模型记忆补齐事实。
